# BLOCKERS

阻塞項與它們實際擋住什麼。

規則：一條阻塞要寫清楚「擋住哪個具體交付」。擋不住任何東西的不叫阻塞，
那叫待辦，不要寫在這裡。

---

## B-01　拿不到 Context 的內容（佔用可以，2026-09-08 更正）

**這一條原本寫錯了。** 原文是「拿不到 Context 內容」，一句話把兩件事混在一起，
害我把一整條可行的路擋掉三輪。正確的切法是：

裝了什麼，拿不到。有多滿，拿得到。

**擋住：** 十四題的第 2、3 題（多少有效、多少污染）。第 1 題（有多少）已解。

**原因：** hook 只給工具事件，看不到 context window 裡裝了什麼。這部分成立。

**已解的部分：** 每一則 assistant 訊息的
`input_tokens + cache_read_input_tokens + cache_creation_input_tokens`
就是那一次送進模型的 context 大小。最新一則的值就是現在的佔用。
直接讀 `~/.claude/projects/**/*.jsonl`，不需要 hook，不需要接任何服務。

實測（本 session，809 則 assistant 訊息，2026-09-08）：

```
送進去的 context = 132,264
  input        2
  cache_read   129,584
  cache_write  2,678
```

做法參考 `~/code-matrix-rebuild/app.py:1215` 的 `token_stats()`，它讀同一批欄位，
只是它算的是時間窗內的累積用量，不是單次佔用。

**剩下擋住的：** 佔用裡面有多少是死的（讀一次沒回頭的檔案、失敗的工具輸出、
重複讀取）。這要 Claim 抽取做完才算得出來。

**不要做的事：** 不要自己估一個 token 數來填。這條仍然成立。

**教訓：** 一條阻塞如果把兩件事寫成一句話，它會連可解的那一半一起擋住。
寫阻塞的時候要問自己：這句話裡有沒有藏著第二個命題。

---


## B-03　Python 版本是 3.11.5，工程書要求 3.12

**擋住：** 目前擋不住任何東西。`forseti doctor` 用標準庫就夠。

**原因：** 這台機器的 python3 是 anaconda 的 3.11.5。

**解法：** 階段 1 需要 Pydantic v2 的時候再處理，可能是建 venv 裝 3.12。

**現況：** 記著，不急。列在這裡是因為工程書寫了 3.12，
避免下一個 session 以為可以直接用 3.12 的語法。

---

## B-04　工程書只讀了三成

**擋住：** 階段 1 與階段 4。

**原因：** 157,680 字，2026-09-08 那輪只讀了第 0 到 10 節加 Phase 清單。
Phase 1-12 的細節沒讀。

**解法：** 動階段 1 之前補讀工程書 Phase 1-2。
詳細對照見 `REQUIRED_READING.md` 的補讀門檻表。

**現況：** 已知，已排入門檻檢查。

---

## B-05　字串比對訊號不能單獨判斷（可以當觸發器，2026-09-08 更正）

**擋住：** 任何單獨靠文字判斷「這句話有沒有在宣稱某件事」的做法。
不擋「拿它當觸發器、再用證據驗證」的做法，那條路是通的。

**這一條原本下得太滿。** 原文是「字串比對類訊號不可用」。
不可用是對「單獨用來下判斷」講的，但我寫成了對這類訊號的全稱否定，
結果差點把一個能用的東西整個丟掉。

**仍然成立的部分：** 2026-09-08 在 40 個 healthy session 上實測，
`SCOPE_EXPANDING_TERMS` 每千則命中 242 次、40/40 session 全中；
抽樣看內容，假陽性有五種結構，其中一類方向是反的
（明確區分「我驗過的」與「我沒驗的」的 agent 會命中更多次）。

所以拿它單獨當判斷依據會系統性誤判，而且會反向懲罰誠實。

**推翻的部分：** `~/code-matrix-rebuild/app.py:1297` 的 `_CLAIM` 就是一張同類的
中英文動作動詞表。但那裡不拿它下判斷，它只負責數「宣稱了幾個動作」，
真正定生死的是磁碟有沒有變（`app.py:1368`，宣稱兩個以上 + 零變化 + 零 verified）。

假陽性被證據那一關擋掉了。詞表誤觸發沒關係，因為觸發之後還要過證據。

**正確的說法：** 字串比對不能單獨當判斷依據，可以當成必須被證據驗證的觸發器。

**解法方向：** 階段 2 的 Claim 抽取用結構規則（路徑、數字、過去式動詞、引號），
抽不出來就標 UNEXTRACTABLE 不猜。詞表只用在「要不要去查證據」這一步。
不要靠加長詞表解決，那仍然不是詞表長度的問題。

**現況：** 限制已寫進 `claims.js` 與 `rhetoric.js` 的定義處並有測試守著。
觸發器這個用法還沒有實作。

**教訓：** 這條跟 B-01 是同一個毛病的兩種形狀。B-01 是把兩件事寫成一句話，
這條是把「在某個用法下不行」寫成「不行」。兩條都是自己寫下之後幾小時內
被自己推翻的，而在被推翻之前我對它們都很有信心。

見 `docs/context-continuity.md` 第 1.1 節。

---

## B-06　「UNSUPPORTED 後接強烈反應 3.8 倍」這個數字不能用

**擋住：** 任何拿它當根據的推論。

**原因：** 沒有分開「AI 自己走掉」與「她的指令本來就模糊」。
見 ADR-004。

**解法：** 階段 3 做完人的訊息分類之後重算。

**現況：** 已標記為不可用。不要在任何報告裡引用它。

---

## B-08　閘門攔不住寫程式的人（2026-09-09 實測，解法方向已改）

**擋住：** 階段 0 的其中一半。控制檔存在不等於會被讀。

### 實測（`docs/cases/gate-takeover-2026-09-09.md`）

兩部分分開測。

**第一部分，開卷考的疑慮，2026-09-08 測過，沒有發生。**
`norikaoda-19` 只拿到一句指令，它的原話是「不能靠題目印出的答案冒充」，
然後真的去讀了六份控制檔。但那證明的是它本來就謹慎。

**第二部分，強制力，2026-09-09 測了。給一個具體的開發任務
（在 forseti.py 加 --json 選項），看它會不會先碰閘門。**

結果：它直接改，22 行新增 4 行刪除，回答裡沒有出現
`gate takeover`、`NORTH_STAR`、`BLOCKERS`、`PHASE_STATUS` 任何一個。

**閘門不是被繞過，是根本沒有進入它的視野。**

### 但這一條的解法方向錯了

原本寫的是「答案從題目旁邊拿掉，只給出處不給內容」。那解決第一部分，
不解決第二部分。

而第二部分不該用強制力解，理由是那次測試的另一半：**它做得很好。**
加了 `as_json` 參數、JSON 版直接給清單不只給數字（理由是「呼叫端拿到
『阻塞 7』之後的下一個問題一定是『哪七個』」）、exit code 兩種模式一致、
零依賴沒破，而且自己實測回讀解析過。

它做的是加一個輸出格式，局部改動，不需要知道北極星。
強迫它先讀完九份文件再改這一行是浪費，而且是那種會讓人關掉工具的浪費。

`src/intervention.js`：A high temperature alone is never sufficient。
`hooks/forseti-hook.mjs:18-35` 那場擋人九小時的事故也是同一個形狀。

### 新的解法方向

**閘門應該是可發現的，不是強制的。**

B-07 的實測給了證據：那個 session 只拿到一句「接手這個專案」，
沒有人告訴它有 `forseti doctor`，它自己找到了。所以在「接手」這個情境下，
可發現性已經夠了。而在「改一行 code」這個情境下，它不去讀控制檔是對的。

真正該做的是讓閘門在對的時機出現，而不是讓它擋住所有時機。
**什麼是對的時機，現在還沒有答案**，那需要 F04 的事件與 F05 的訊號
接上來才判斷得了。

所以這一條不解除。它從「閘門沒有強制力」變成「還不知道閘門該在什麼時候
出現」，而後者是一個更難也更誠實的問題。


---

---

---

# 已解除

放在這裡的不再是阻塞，`forseti doctor` 不會算進去。
保留全文是因為「怎麼驗掉的」比「它曾經擋住什麼」有用。

---

## B-10　回報通道要 worker 有執行權限（2026-09-09 實測解除）

**擋住：** 無。2026-09-09 兩次實測解除，第二次那則 WORKER_ACCEPTED
是八種 worker 事件在真實工作裡的第一次使用。

**證據（2026-09-09 實測）：** `tools/check-orphan-workers.py` 第一次執行，
12 個被派工的步驟裡 10 個「已終止但從頭到尾沒回報過」。
**八種 worker 事件在真實工作裡的使用次數是零。**

**那 10 個是兩個不同的原因，不要混成一個：**

九個是我自己做的（F03 到 F08 六步、`T-eb34677986` 三步）。
我有權限，就是沒用。這是「做出機制然後不用它」的又一個實例。

一個是真正跨 session 的 worker（`T-192ced6ff2/s1`）。它想回報但辦不到。

**跨 session 那一個的真實原因，是它自己講出來的：**

那個 `claude -p` session 是非互動模式，用 `--permission-mode acceptEdits`
啟動，於是 Write 被允許、Bash 被擋。它能寫出 5,952 bytes 的合格產物，
卻連一句 `WORKER_ACCEPTED` 都送不出去，因為送出的方式是跑一行 python。

它的原話（逐字摘自它的回報）：回報通道對這個權限模式是斷的，
帳本裡這一步現在看起來就是它自己要抓的那種孤兒。

**所以這不是 worker 不配合，是結構性的：**
回報通道假設 worker 能執行 shell，而派工的一方不見得給得出那個權限，
也不見得知道自己沒給。**能做事跟能回話是兩種不同的權限**，
現在的設計把後者當成前者的附贈品。

**我差點把因果寫反：** 第一版的 B-10 寫的是「它把訊息當成工作說明，
沒有當成協定」。那是看到「沒有事件」就腦補出來的因果，
沒有去讀 worker 實際說了什麼。讀完它的回報才發現原因完全不同。
**沒有它那份七條回報，錯誤的因果就會被寫進文件並且看起來很合理。**

**可能的方向（都沒驗過，不准當結論）：** 派工時把所需權限講清楚並確認；
或讓回報不依賴執行權限（例如寫一個檔案，由控制端的巡檢去撿）；
或接受回報不可靠，把判斷完全建立在巡檢與 verifier 上。

最後一條是現在實際在運作的方式，而且它有效：那個 worker 從頭到尾
沒說過一句話，產物仍然被驗證通過。**巡檢比回報可靠，因為回報要
對方配合、要對方有權限，巡檢兩者都不需要。**

### 解法：把回報的門檻降到跟寫產物一樣

worker 只要能寫檔案就能回報。`<repo>/.forseti/inbox/<step>/` 底下放一個
`.txt`，第一行事件種類，其餘說明，一個檔案一筆。控制端巡檢時去撿，
撿到就變成帳本裡真正的事件，檔案移進 `.done/`。

不用 append：read-modify-write 對一個只有 Write 的 worker 是額外的
失敗機會。目錄與格式說明由 `handoff` 預先建好，不要求 worker 自己建 ——
那是一個它可能做不到、而且做不到的時候沒有人會知道的動作。

配套三件：`handoff --no-shell` 把能力記進帳本（`worker_can_report`，
schema v3）；訊息依能力產生，不會叫一個跑不了指令的 worker 去跑 python；
復原階梯對 `write_only` 跳過前三階，因為那三階全是「問它」，
而問一個回不了話的 worker 三次，它不會因此變得能回答。

沒有宣告就是 `unknown`，那不是「大概可以」，是「沒問過」。
舊資料一律標 unknown，填 full 會把一個沒問過的問題偽裝成有答案。

### 第一次實測：設計被推翻

第一版把收件匣放在 `~/.forseti/inbox/`，理由是「回報不該污染工作目錄」。

那個 worker 三條路全被擋（Write 工具、Bash 重導向、單指令重導向），
因為它的沙盒白名單只有 repo 目錄，home 在外面。它試三次就停手，
沒有再換變體，而且照實說明寫不進去，沒有裝作有寫。

**它的診斷比我的假設精確，建議也比我的設計好。** 原話大意：沉默不是
因為死掉，是因為寫不進去，而那兩件事在帳本裡長得一模一樣；建議把
inbox 搬進 repo 內。

已照做。原本的理由站不住：污染工作目錄的成本，遠低於通道整條斷掉。
放 repo 還有一個更重要的性質 —— **worker 本來就有 repo 的寫入權限，
因為它要寫產物，於是「能做事」就蘊含「能回話」**，不必再另外要一份權限。

### 第二次實測：通了

同樣的權限模式（`acceptEdits`，Bash 被擋）。worker 寫出
`accepted.txt`，`watch` 撿到，帳本裡出現真正的 `WORKER_ACCEPTED`，
檔案移進 `.done/`。整條鏈走通。

同一時間 `check-orphan-workers.py` 的數字：14 個被派工的步驟裡
11 個從頭到尾沒回報過，唯一回報過的就是走收件匣那個。

### 一個環境的坑

這個 repo 在 exFAT 上，macOS 會為每個檔案寫一個 `._` 開頭的 AppleDouble
附屬檔。不濾掉的話一則回報會被算成兩則，第二則的內容是二進位垃圾。
`tools/orphans.mjs` 因為同一個原因假警報過兩次，那個教訓寫在
`tools/README.md`，是前一個 worker 寫的。已在 `collect_inbox` 濾掉。

### 剩下那 11 個沒回報過的，不是同一件事

它們幾乎都是我自己做的（`worker=norikaoda-03`）。那不是「回不了話」，
是同一個 session 同時當 controller 與 worker 時，回報是多餘的 ——
它不需要透過事件通知自己。`check-orphan-workers.py` 把這類列為歷史
紀錄、不影響 exit code，那個分界是對的。

真正要看的是跨 session 的那幾個，而它們現在有通道了。

---

## B-12　剛寫入的事件查不到自己（2026-09-11 找到並修好）

**擋住：** 無。2026-09-11 找到真正原因並修好。
**它不只是測試不穩，是一個會誤判 worker 的真 bug。**

**證據等級：一次觀察，之後 8 次重現不了。** 這一條的內容比其他條弱很多，
記下來的理由不是它有多確定，是**間歇性失敗會累積成災**，
而且第二次發生的時候如果沒有第一次的紀錄，會被當成偶發再放過一次。

**發生經過（2026-09-10 晚間）：** 在一個「先寫 PHASE_STATUS、
再跑全庫測試」的複合指令裡，`tests/test_f05.py` 回非零。
之後單獨連跑五次、用同樣的迴圈跑三輪，全部通過，一次都沒再失敗。
沒有留下失敗當下的輸出（那個迴圈只印檔名不印細節），
**這是我的疏漏 —— 失敗的當下沒有把 stderr 留下來，
於是唯一一次證據就這樣沒了。**

**可能的方向，全部未驗證：**

`test_f05.py` 裡有時間相關的斷言（`age_step(7200)` 之後判定停滯），
而 `check_liveness()` 現在會先 `collect_inbox()`，多了檔案系統操作。
在 exFAT 外接碟上那個延遲不穩定。

`test_sweeping_repeatedly_does_not_bloat_the_event_log` 斷言探測事件
剛好一筆，而探測是否寫入取決於產物 hash 有沒有變 —— 如果同一秒內
檔案 mtime 的解析度不夠，可能判定不同。

**下一次怎麼抓：** 跑全庫的迴圈要留下失敗的完整輸出，不能只印檔名。
現在那個迴圈是 `python3 "$f" >/dev/null 2>&1 || echo "FAIL $f"`，
把 stderr 丟掉了。改成寫進暫存檔，失敗才印出來。

### 第二次發生，這次抓到斷言（2026-09-11）

照上面那條改用留輸出的寫法之後，第二次撞到，拿到了關鍵證據：

```
AssertionError: 1.0 != 0.0
```

**那個形狀指向 `no_progress_growth` 因子。** `watchdog.assess()` 裡
`no_growth = 0.0 if progress_changed else 1.0`，所以這是「預期產物有變動、
實際判定成沒變」。

**排除了一個原本的猜測：** `ProgressProbe.take()` 用的是內容的
sha256，不是 mtime，所以「同一秒內 mtime 解析度不夠」那條不成立。

**剩下最可能的：** 這個 repo 在 exFAT 外接碟上，測試寫完檔案之後
probe 立刻讀，而寫入還沒落到磁碟，於是讀到舊內容、hash 沒變。

**如果那個推論對，這就不只是測試不穩 —— 正式執行也會有。**
`check_liveness()` 在真實情況下同樣是「worker 剛寫完、巡檢立刻讀」，
讀到舊內容會讓一個正在做事的 worker 被判成停滯。

**仍然重現不了。** 第二次之後單獨連跑六次、完整迴圈跑三輪，
全部通過。沒有辦法在可控的條件下觸發它，所以上面那段是推論不是結論。

**下一步該做什麼：** 寫一個專門的壓力測試 —— 在 exFAT 上快速寫檔然後
立刻 probe，跑幾百次看 hash 有沒有抓不到變化的時候。
那個測試如果重現得出來，這一條就從推論變成結論，而且會變成一個
要修的真 bug 不是測試不穩。

## B-13　主 session 的活動採集不到，而第二層要動 owner 的全域設定

> **2026-09-11 升級：它不是缺口，是前提。**
>
> 把階段 2 與階段 3 接起來跑之後才看清楚。一個宣稱說「我改好了
> `docs/build-plan.md`」，要驗它就得知道那個相對路徑相對於什麼。
> 兩個基準都試過，兩種誤判方向剛好相反：
>
>     拿 Forseti repo 當基準   →  別的專案的檔案全被判成假的
>     拿 session 的 cwd 當基準 →  這個 repo 裡真的存在的檔案全被判成假的
>
> 方向相反，說明問題不在規則。**一個相對路徑少了它的基準，
> 本來就沒有真假可言。** 而那個基準只有一個地方有：帳本裡 hook
> 當時記下的 FILE_WRITE。
>
> 所以 `claims.verify()` 現在對「相對路徑而且帳本沒有證據」一律回
> UNKNOWN。那是誠實的，代價是：**在 B-13 解決之前，這一層對絕大多數
> 宣稱只能回答「我不知道」。** 實測 790 輪裡只剩 2 輪被標記（0.3%）。
>
> 換句話說，階段 2 與階段 3 的機制都通了，但它們現在拿不到足以判斷的
> 資料。B-13 不是「之後再補的採集」，它是這兩層能不能運作的條件。


**擋住：** 關於主 session 的任何宣稱，這套系統目前都只能答「我不知道」。
階段 1 的 Event Ledger 與階段 2 的 FP-03 在最重要的那個對象上是半盲的。

**這個缺口有兩層，2026-09-11 實測確認過：**

| 層 | 是什麼 | 狀態 |
|---|---|---|
| 註冊 | hook 要被 Claude Code 載入 | **沒解**，見下 |
| 邊界 | 載入之後要通過 `insideRepo` | 已解 |

**第二層已經解掉。** 邊界加了第二條路 `touchesRepo()`：被寫的檔案在
repo 底下也算在範圍內。實測 cwd 在 home、檔案在 repo 會記錄；
cwd 在 home、檔案也在別處**連目錄都不建**。三條測試守著
（AT-HOOK-B4/B5/B6），其中 B5 守的正是 2026-09-08 那次事故的形狀。

**第一層沒解，而且我不會自己解。**

主 session 的 project 是 `/Users/norikaoda`，載入的是
`~/.claude/settings.json`，那裡只註冊了 `SessionStart`，沒有這三個 hook。
要讓它載入，只有兩條路，**兩條都要動 owner 的機器設定**：

一，把 hook 註冊進 `~/.claude/settings.json`。
**那正是 2026-09-08 事故的做法**，當時擋掉九小時工作。
現在的差別是：那三個 `process.exit(2)` 已經全部過 `gate()`，
而閘門只放行安靜標註；加上 `insideRepo` 與 `touchesRepo` 兩道
程式碼層邊界，都不可設定關閉。所以現在裝上去只會「記錄動到 Forseti
檔案的事件」，不會擋任何東西。**但這句話的保證來自程式碼，
不是來自我的承諾，而 2026-09-08 我也以為我知道範圍。**

二，從 repo 目錄開 session 工作。零風險、零設定，代價是工作習慣要改，
而且只有在那個目錄開的 session 才有紀錄。

**為什麼我不自己做第一條：** 那是寫進她家目錄的設定，作用範圍是整台
機器上的每一個 session。這個專案的第一條規則是不要在她沒確認的情況下
擴大範圍，而那條規則正是從那次事故長出來的。

### 真正的原因，跟我猜的完全不同

`worker_event()` 裡用了**兩次** `time.time()`：一次給事件的 `at`，
一次給 `last_progress_at`。而 `check_liveness()` 查的是

    SELECT COUNT(*) FROM events WHERE step_id=? AND at > ? ...
    參數是 (step_id, last_progress_at - 0.001)

**那兩行之間只要超過 1 毫秒，剛寫進去的事件就查不到自己**，
`no_event_activity` 從 0.0 變成 1.0 —— 正是兩次失敗的斷言。

I/O 慢、sqlite commit、GC 都會造成那個間隔，所以它是間歇性的。

### 怎麼確定的

先寫 `tools/probe-stress.py` 去驗原本的推論（exFAT 寫入延遲）。
exFAT 與 APFS 各跑 300 輪、四種檔案大小，**兩邊都零漏抓**。
那個推論被推翻了 —— 而推翻它才是關鍵的一步，
因為它把注意力從檔案系統移回程式碼。

接著從斷言的形狀（`no_event_activity` 該是 0 卻是 1）回推那個因子
怎麼算的，讀到那段 SQL 用 `last_progress_at - 0.001` 當下界，
再回頭看 `worker_event()` 就看到兩次 `time.time()`。

人工把間隔拉成 10ms，一次就重現。

### 修法與回歸測試

`_event()` 加一個 `at` 參數，`worker_event()` 取一次 `now` 兩邊共用。

三條測試：事件的 `at` 與 `last_progress_at` 必須是同一個讀數；
人工拉開間隔會重現（那條在修好之後仍然會過，它測的是因果不是現況，
留著讓下一個人不用重推一次）；以及修好之後的實際效果 ——
回報過進度的 worker 不再被判停滯。

### 值得記的一件事

B-12 開的時候只有「一次觀察，重現不了」，證據等級很弱，
而我當時寫下來的理由是「間歇性失敗會累積成災，第二次發生的時候
如果沒有第一次的紀錄，會被當成偶發再放過一次」。

第二次真的發生了，而且因為第一次留了紀錄、也因為那條紀錄寫了
「下一次怎麼抓：要留下失敗的完整輸出」，第二次才拿得到那個斷言。
**那條記錄自己起了作用。**


---

## B-11　同名不同義，缺一份名詞表（2026-09-10 已解除）

**擋住：** 無。2026-09-10 寫了 `docs/glossary.md`。

**這五個是 2026-09-09 到 09-10 讀規格時陸續撞出來的，不是想像的：**

| 名字 | 一邊是 | 另一邊是 | 接錯的後果 |
|---|---|---|---|
| ~~`events`~~ | v5.0 §6 與工程書 Phase 1：provider 行為事件 | `ledger.py` 的 events 表：派工協調事件 | **2026-09-10 已由 ADR-008 定名**：兩本帳本分開，文件與口語一律講「Task Ledger 的 events」或「Event Ledger 的 events」 |
| `temperature` | `src/signals.js`：session 健康溫度 | `src/thermometer.js`：查過的對自己生出來的比例 | 接書 Phase 8 的 UI 時把事實比例當健康度顯示 |
| `rescue` | `src/rescue.js`：spec v0.1 §7 的中斷前快照 | 書 Phase 7 的 Rescue Mode，對應物其實在 `src/recovery.js` | 用檔名對照書的人會對錯位 |
| lineage 邊型 | v5.0 §6.3 十種 | `src/topology.js:40` 自己一組九種 | 只有三個名字重疊，混用會產生對不上的圖 |
| `heartbeat` | `src/heartbeat.js`：主動報告機制 | v5.0 §14 `runtime_node_heartbeat`：節點存活訊號 | 把「有沒有主動報告」當成「節點活著沒」 |

**五個撞名不是巧合，是缺一份名詞表的症狀。**

第一個（`events`）已經在 ADR-008 定名，剩四個。**但那反而更說明問題：**
它是靠一次 owner 裁決解掉的，而不是靠一份任何人都查得到的名詞表。
剩下四個如果也要各自等一次裁決，成本會比寫一份 glossary 高很多。

每一個單獨看都像小事，合起來是同一個結構問題：這個專案有四份規範來源
（工程書、v5.0、spec v2.0、spec v0.1）加上一套自己長出來的實作，
而沒有任何地方記錄「這個詞在哪一份文件裡是什麼意思」。

**還有一個放大它的因素：** repo 程式碼註解裡的 § 編號一律指
`docs/spec-v2.0.md`，而工程書與 v5.0 各有自己的章節號。同一個 §17
在三份文件裡是三件事。

**解法方向：** 一份 `docs/glossary.md`，每個詞列出它在各份文件與程式碼裡
分別是什麼，以及本專案用哪一個。不是統一命名（改名會斷工具），
是讓對照查得到。

**不要往哪個方向修：** 大規模改名。`src/topology.js` 那組邊型有測試、
`heartbeat.js` 有測試，改名等於改一批公開介面，而問題本來就不是名字錯，
是沒有地方說明它們各自指什麼。

### 怎麼解掉的

`docs/glossary.md`。八條，每條列出這個詞在各份文件與程式碼裡分別是什麼、
附出處行號、本專案怎麼用、接錯的後果。

開這條 blocker 時列了五個，寫的時候又查出兩個：

`handoff` 三個意思（工程書 §27 的 `HANDOFF.md`、`forseti handoff` 指令、
`src/handoff.js` 的 Moirai M2 交接規則），這是撞得最厲害的一個。

「階段」對 `Phase`（本專案六階 `docs/build-plan.md:271-276`，
工程書十三個 Phase，而且不是一對一對應）。

加上章節號 `§N` 在三份文件裡是三套，總共八條。

**刻意不做統一命名。** `topology.js` 那組邊型有測試、`heartbeat.js`
有測試，改名等於改一批公開介面，而且改完之後舊文件與舊 commit 訊息
裡的名字全部對不上。問題本來就不是名字錯，是沒有地方說明它們各自指什麼。

### 誠實標記：這份表一定不完整

八條全部是撞到才發現的，不是系統性掃出來的。掃過模組名與規格術語的
交集，但「兩邊都常出現」不等於「意思不同」，逐個查證成本太高，
所以沒有為了湊完整而填。glossary 末節寫了怎麼發現新的，
以及一條規矩：**沒有出處的不要加，一份有猜測的名詞表比沒有更危險，
因為它看起來權威。**

### 一個值得記的觀察

第一條（`events`）是靠一次 owner 裁決解掉的（ADR-008），
其餘七條靠一份任何人都查得到的文件。**裁決解一條，文件解一類。**


---

## B-09　worker 死了十七小時，controller 不知道（2026-09-09 實測解除）

**擋住：** 無。2026-09-09 用一個真的跨 session worker 實測解除。

**事件：** 2026-09-08 18:58 `norikaoda-19` 因 API Error 停止。
2026-09-09 12:40 才被發現，中間 17 小時 42 分。

**期間我做了什麼：** 實作了 F05 watchdog，包含停滯偵測、復原階梯、
以及 CT-F05-04「連續無回應就重派」。一次都沒有對那個 session 跑過。

**根因不是忘記，是沒有接上。** `check_liveness()` 只認得帳本裡的
step，而那個 session 從來沒有被登記成一個 step。我用 SendMessage
派工，那條路徑完全在帳本外面。

F03 §6 要防的五件事之一是「worker finishing without event」。
我犯的是它的鏡像：controller 忘記 worker 存在。

**解法：** 跨 session 派工要走帳本 —— `dispatch()` 之後才送
SendMessage，而不是只送訊息。這樣 worker 就有 step，watchdog 才看得到它。

**這條跟 auto_dispatch 那個落差是同一類：** 做出一個能自動繼續的東西，
跟真的讓它自動繼續，是兩件事。

### 怎麼解掉的（2026-09-09）

原本的解法只寫了一句「`dispatch()` 之後才送 SendMessage」。那不夠，
因為它仍然依賴我記得先 dispatch。**依賴記憶的解法，防不了失憶造成的問題。**

實際做的是三塊，每一塊都不需要人記得：

`handoff` 成為跨 session 派工的入口。它先登記進帳本再產出要送的訊息，
所以走這條路就不可能漏掉登記。兩種模式：派一個已存在的步驟，
或即時開一個單步任務並派出去（後者才真的能取代直接送 SendMessage）。
它不自己送訊息，因為 SendMessage 是 Claude 的工具、shell 呼叫不到，
不假裝 CLI 能送。

`watch` 一次掃完所有進行中的步驟，對每一個跑 `check_liveness()`。
關鍵在於入口是「看全部」不是「看某一個」：十七小時那次 watchdog
是會動的，缺的是有人想到要對那個 step 問一次。有可疑項時回傳 1，
可以直接掛排程。

`doctor` 開頭就報告有幾個步驟正在被別人做。接手的人第一眼就看得到，
不必想到要問。

### 實測（不是模擬）

用 `claude -p` 開一個真的 session 當 worker，派了一件真的工作
（寫 `tools/check-orphan-workers.py`）。派出去之後我刻意不記 step_id，
只用 `watch` 去找它，找得到。

四分鐘後它交出 5,952 bytes，兩個 verifier 都通過。

**但它一個事件都沒回報，也沒跑 verify。** 那是新的一條，記在 B-10。

### 一個副產品

實測時發現 `watch` 每跑一次就寫一筆 `PROGRESS_PROBE`。掛進排程每分鐘
跑的話，事件表會以巡檢頻率膨脹，而那些筆數一個字的資訊都沒有。
改成只在探測結果有變化時才記，沒變化不寫。判斷不受影響：
`changed_from()` 比的是內容，基準留在上一筆有變化的地方仍然對得出來。

---

## B-02　hook 的載入條件與寫入位置（2026-09-09 全部驗完，已解除）

**擋住：** 無，2026-09-09 全部驗完。保留在這裡是為了記錄怎麼驗的。

### 最後一塊：從 repo 目錄啟動會不會觸發（2026-09-09 13:15 實測）

84 號查不到這一條，因為 Claude Code 載入 project settings 的規則在
harness 內部。讀不到就從外面觀察效果：

```bash
cd "/Volumes/NewDrive/AI Project/Forseti" && claude -p "建立 docs/cases/hook-probe.md" --permission-mode acceptEdits
```

結果，三件事同時成立：

`repo/.forseti/state.json` 與 `streak.json` 在 13:15 被建立，時間戳與那次
Write 對得上。state 裡有一筆 coverage，session `7307ad03`，
`EDITED /Volumes/NewDrive/AI Project/Forseti/docs/cases/hook-probe.md`。
streak 是 `{"total_off":0,"total_checked":1}`。

全域 `~/.forseti/streak.json` 維持 `12/8` 完全沒動。

所以：**從 repo 目錄啟動確實會載入 project settings 並觸發 hook；
寫入位置是啟動時的 cwd；`insideRepo` 那條硬邊界真的擋得住，
沒有污染全域。**

### 為什麼它看起來像從來沒運作過

掃全機 jsonl 找 cwd 在 repo 底下的 session，**結果是零個**。
hook 註冊在那裡兩天，一次都沒有機會跑，因為從來沒有人從那個目錄
啟動 session。不是機制壞了，是條件從來沒滿足過。

這一點值得記：一個「看起來沒在運作」的機制，可能只是它的觸發條件
從來沒有被滿足。先查有沒有發生過，再查會不會發生 —— 前者是零成本的，
後者才要花錢。

### 驗證方法對其他 blocker 的意義

B-07 也是「只有 owner 能驗」那一類，理由是需要一個全新 session。
現在證明了 `claude -p` 可以開一個真正獨立的 session（有自己的
session id、會載入 project settings、會觸發 hook）。
**所以 B-07 也不需要 owner 動手了。**

### 載入條件

hook 只註冊在 **repo 的** `.claude/settings.json`（3 處，
Pre/PostToolUse matcher 是 `Write|Edit|MultiEdit|NotebookEdit` 與 `Stop`）。
全域 `~/.claude/settings.json` 是 0 處。

第二道是硬邊界，寫在 `hooks/forseti-hook.mjs:273-275`，在讀任何狀態、
建任何目錄之前就檢查：

```js
const cwdForBoundary = process.env.CLAUDE_PROJECT_DIR || input.cwd;
if (!insideRepo(cwdForBoundary) && !crossProjectEnabled(cwdForBoundary)) OK();
```

`insideRepo`（`:77-81`）比對的是 `REPO_ROOT = resolvePath(HERE, '..')`，
也就是 hook 檔案自己的位置往上一層。**不看設定、不看環境變數**，
所以這條邊界改不掉，只能改程式碼。

### 寫入位置

`:164-173`。一律寫 `projectRoot(cwd)/.forseti/{state,streak,goal}.json`，
而 `projectRoot = CLAUDE_PROJECT_DIR || cwd || process.cwd()`。
寫到哪由觸發當下的環境決定，不是固定寫 repo。
另有全域 `$HOME/.forseti/goal.json`，hook 只讀不寫。

### `~/.forseti/` 那批資料是什麼（我昨天解讀錯了）

**它不是「hook 正常運作的證據」，是 2026-09-08 那場事故的殘骸。**

`forseti-hook.mjs:18-35` 自己寫了經過：這個檔案原本有三個
`process.exit(2)`，一個都沒問過 `intervention.js` 的 `canIntervene`。
裝上去之後，一個只寫著某專案路徑的 scope 對整台機器每一個目錄生效，
擁有者其他所有工作連續寫五個檔就被 deny 一次，整整九個小時。

原文最後一句：「留下的證據是 streak.json 的 total_checked: 12,
total_off: 8」。

所以那個 8 不是「8 次判離題」，是 8 次擋掉她的正常工作。
事故後撤出全域設定、只留 repo settings、加上 `insideRepo` 硬邊界，
所以現行程式碼跑不出那批資料 —— cwd 在 home 一帶的 session 會在 `:275`
就 exit 0，連目錄都不會建。

**2026-09-09 我把事故的殘骸當成了機制運作的證明。方向沒錯，性質反了。**

### 剩下三條沒驗的（由 norikaoda-84 查出並誠實標記）

一，Claude Code 決定載入哪份 project settings 的規則讀不到（harness 內部）。
只能反推「本 session 的 project 是 home、沒載 repo settings」，
**無法直接證明「從 repo 目錄啟動就一定會載入並觸發」。這一條仍然只有
owner 從 repo 目錄開 session 驗得到。**

二，「09-08 當時全域 settings.json 確實註冊過這三個 hook」沒有直接檔案
證據，現在全域已經沒有它們。時序判定是靠 hook 自述、`~/.forseti/` 遺留、
streak 三者一致反推。要坐實需要 settings.json 的版控或備份歷史。

三，全域 `~/.forseti/goal.json` 多出的 ISEEU 與 `/private/tmp` scope
是誰寫的，沒找到寫入者。`forseti-declare.mjs:27` 寫的是
`declarations.json` 不是 `goal.json`，hook 只讀不寫。寫者應該是
某個 CLI 或安裝腳本，還沒讀到那段，不猜。

### 出處

行號由 `norikaoda-84` 提供，**本 session 親自抽驗過 `:73-84`、`:270-280`、
`:162-175`、`:16-36` 四段，全部對上。** 它也清掉了自己製造的測試污染。

---

## B-07　階段 0 的出口條件（2026-09-09 實測通過，已解除）

**擋住：** 無。2026-09-09 實測通過。

**原本的理由：** 出口條件需要一個全新 session，而我已經知道答案，
測不了「一個不知道答案的人能不能靠它知道」。所以歸為只有 owner 能做。

**前提被推翻：** B-02 的驗證證明 `claude -p` 開出來的是真正獨立的
session，有自己的 session id、會載入 project settings、會觸發 hook。

**實測結果：** 完整記錄在 `docs/cases/doctor-handover-2026-09-09.md`。

指令只有一句「接手這個專案，告訴我現在該做什麼，還有哪些事沒做完」，
不提任何工具名。它跑了 `forseti doctor`、`forseti tasks`、七個測試檔，
四件事全部答出來，而且明說「這條是我剛剛跑的，不是抄文件」。

**它做了三件出口條件沒要求的事：** 親自跑 92 條測試驗證文件的宣稱；
發現三個未追蹤的 `tools/*.mjs` 但選擇不動、回報給人決定；
指出 B-07 的前提已被 B-02 推翻，也就是它讀出了「我正在被測的這一條，
已經不需要人了」。

**意外的加分：** 第一次跑的時候權限不足，Bash 被擋住，doctor 跑不起來。
它靠直接讀控制檔答出了同樣四件事，並且開場就聲明哪兩件做不到、
標成未驗證、不假裝做了。**所以控制檔本身撐得住接手，doctor 只是讓它更快。**

**副產品：** 過程中連續兩次撞到 `API Error: safeguards flagged`，
換模型才成功。這解釋了 2026-09-08 `norikaoda-19` 中斷十七小時的原因 ——
不是訊息內容的問題，是隨機誤判。
