node_modules/
.DS_Store
*.log
# 執行期狀態不進版控,但北極星要。
# 搬碟的時候整個 .forseti/ 被忽略,goal.json 沒跟著過來,
# 於是離題偵測在新位置是關的 —— 而且完全靜默,沒有任何跡象。
.forseti/*
!.forseti/goal.json
# 控制檔必須進版控。它們是給下一個 session 讀的,不在版控裡等於不存在,
# 而那正是上面那三行記的教訓,只是換一個檔名重演。
# 執行期狀態(state / streak / declarations / event_ledger)仍然忽略。
!.forseti/NORTH_STAR.md
!.forseti/PHASE_STATUS.md
!.forseti/DECISION_LEDGER.md
!.forseti/BLOCKERS.md
!.forseti/REQUIRED_READING.md
# 2026-09-11 加。那天新寫的 HANDOVER_FAILURE 被 `.forseti/*` 擋在版控外,
# 而我宣稱「已經 commit」——commit 確實成功了,只是不含那個檔案。
# 白名單是「列舉式」的,所以每一份新的控制檔都要自己加一行,漏了不會有錯誤。
!.forseti/HANDOVER_FAILURE_*.md
# 2026-09-16 加。**同一個坑第三次。** 上面那段記的是 HANDOVER_FAILURE
# 被擋在版控外,而 ROADMAP.md 與 AUTO_CONTINUE_LOG.md 一直也被擋著,
# 沒有人發現 —— 因為漏了不會有錯誤,只會安靜地不存在。
# 這兩份是控制檔不是執行期狀態:ROADMAP 是開發順序(它自己寫著
# 「這份取代 TODO_AUDIT 的清單部分」),LOG 是每一輪做了什麼的紀錄。
# 不在版控裡等於下一個 session 拿不到。
!.forseti/ROADMAP.md
!.forseti/AUTO_CONTINUE_LOG.md
# NEXT.md **刻意不加**,留給 owner 決定。
# 它是 handoff.py 每 4 分鐘自動重寫的執行期產物(性質同 state.json),
# 進版控會產生大量沒有意義的 commit。但它同時是「下一個接手的人
# 唯一看得到的東西」,所以這是一個取捨不是一個事實,我不自己定。
# 要它進版控的話,加一行 `!.forseti/NEXT.md` 就好。
# 產品方向。owner 2026-09-11 逐字交代的三件事,不能只活在 context 裡。
!.forseti/PRODUCT_DIRECTION.md
# Widget 設計規格。owner 2026-09-13 到 09-14 口述的設計意圖正本。
!.forseti/WIDGET_SPEC.md
._*
.DS_Store

# 端到端測試的沙箱,建在 repo 底下是因為 hook 的邊界只在 repo 內作用
.tmp-e2e-*
.tmp-*.js

# 校準的中間產物。它們含有 transcript 的完整路徑,而路徑本身
# 就是使用者的專案清單與工作紀錄 —— 這個 repo 是公開的。
#
# 統計數字可以公開(見 docs/calibration/),路徑不行。
# 要重跑的人自己在本機產生,指令寫在校準文件最後。
*profiles.json
negatives.json
corpus-*.json
signals.json
reactive.json
turns*.json
fingerprints*.json

# Python 編譯產物
__pycache__/
*.py[cod]

# B-10 的收件匣。worker 的回報落在這裡,撿完就移到 .done/。
# 不進版控:它是執行期的暫存,不是專案的一部分。
.forseti/inbox/

# 桌面版 build 產物。target 已改指本機 APFS(見 desktop/.cargo/config.toml),
# 這幾條是保險。
desktop/node_modules/
desktop/src-tauri/target/
desktop/dist/
